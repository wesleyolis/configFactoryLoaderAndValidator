{
  "name": "redblade-platform",
  "version": "1.0.0",
  "description": "",
  "repository": "https://bitbucket.org/redbladeio/redblade-platform",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "tsc": "tsc",
    "tsct": "tsc -v;",
    "copy-d-ts" : "find ./src -name  '*.d.ts' | cut -d '/' -f3- | xargs -I {} cp './src/{}' './lib/{}'",
    "copy-js": "rm -rf lib && cp -r src/ lib/; find ./lib -name '*.ts' -type f | xargs rm",
    "copy-js-test": "rm -rf lib-test && cp -r test/ lib-test/; find ./lib-test -name '*.ts' -type f | xargs rm",
    "build": "npm run copy-js && tsc && npm run copy-d-ts",
    "build-test": "npm run copy-js-test && tsc -p tsconfig-test.json",
    "build-all": "npm run build && npm run build-test",
    "test": "mocha --recursive 'lib-test/**/*.spec.js'",
    "test-coverage": "nyc --all mocha --recursive 'lib-test/**/*.spec.js'",
    "build-run-test": "npm run build-test && npm run test",
    "build-run-test-coverage": "npm run build-test && npm run test-coverage",
    "build-all-run-test": "npm run build-all && npm run test",
    "build-all-run-test-coverage": "npm run build-all && npm run test-coverage"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/bluebird": "^3.5.20",
    "@types/ssh2": "^0.5.35",
    "@types/ssh2-streams": "^0.1.2",
    "mongo-in-memory": "0.0.5",
    "typelevel-ts": "^0.2.3",
    "typescript": "^2.8.4"
  },
  "dependencies": {
    "@types/bluebird": "^3.5.20",
    "@types/joi": "^13.0.8",
    "@types/verror": "^1.10.3",
    "bluebird": "^3.5.1",
    "buffer-equal-constant-time": "^1.0.1",
    "config": "^1.29.4",
    "joi": "^13.4.0",
    "redblade-testing": "git+ssh://git@bitbucket.org/redbladeio/redblade-testing.git",
    "redblade-types": "git+ssh://git@bitbucket.org/redbladeio/redblade-types.git",
    "ssh2": "^0.5.2",
    "tsc-simple": "git+https://github.com/fictitious/tsc-simple.git",
    "tslint": "^5.10.0",
    "verror": "^1.10.0"
  }
}
